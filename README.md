# People-Management-System
this system requires nodejs and angularCLI to be properly used 
it will also require internet connection for other plugins like bootstrap

#how to test
after installation of the Node (https://nodejs.org/en/download/) and AngularCLI (https://cli.angular.io/)
  -clone the project
  -open comand prompt and cd into the project's directory
  -$npm install
  -$ng serve